/* it depend on what you want to convert, we can converts ARRAYS into STRINGs and NUMBERS into STRINGs and visa versa

'type of' operator tells us the data type of an object.

if you work with some objects in JS, and at somepoint we need to be sure our data is a string, array or something else, to confirm is a number is a number
*/

let numberOrString = "12";

typeof (numberOrString);

//now we know the data is a string